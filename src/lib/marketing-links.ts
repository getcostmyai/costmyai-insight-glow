/**
 * Outbound marketing destinations.
 *
 * Booking runs on HubSpot Meetings: round-robin across the team is a setting on
 * the HubSpot link itself, so rotating who takes a demo never requires a code
 * change here.
 */
export const BOOK_DEMO_URL = "https://meetings-eu1.hubspot.com/robin-mueller";
